# Local development only. Remote/external binding is intentionally rejected
# until authenticated remote management is implemented.
SWARM_LISTEN_ADDR=127.0.0.1:8080

# Optional absolute path to a Swarm-compatible engine sidecar.
SWARM_ENGINE_PATH=

# Required absolute existing directory when SWARM_ENGINE_PATH is configured.
SWARM_DOWNLOAD_ROOT=
